stage="rsnapshot $level"
ls $sharesDir"/" | while read share; do
  . $eachShare
  if [ "$level" == "Hours" ]; then
    echo "sync $share"
    rsnapshot -c "$rsnapConf" sync
    chkEx
  fi
  rsnapshot -c "$rsnapConf" "$level"
  chkEx
  echo "Snapshot is fine $level"
done
